[+ autogen5 template +]
## Process this file with automake to produce Makefile.in

## Created by Anjuta

INCLUDES =

AM_CFLAGS =\
	 -Wall\
	 -g

bin_PROGRAMS = [+(string-downcase (get "Name"))+]

[+(string-downcase (get "Name"))+]_SOURCES = \
	main.c

[+(string-downcase (get "Name"))+]_LDFLAGS = 

[+(string-downcase (get "Name"))+]_LDADD = 

